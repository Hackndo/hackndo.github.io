---
layout: null
sitemap:
  exclude: 'yes'
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
  xmlns:xhtml="http://www.w3.org/1999/xhtml">
  {% for post in site.posts %}
  <url>
    <loc>{{ site.url }}{{ post.url }}</loc>

    {% if post.translation %}
      <xhtml:link rel="alternate" hreflang="fr" href="{{ site.url }}{{ post.url }}"/>
      {% for lang in post.translation %}
        {% if lang[1] != nil and lang[1] != "" %}
      <xhtml:link rel="alternate" hreflang="{{ lang[0] }}" href="{{ lang[1] }}"/>
        {% else %}
      <xhtml:link rel="alternate" hreflang="{{ lang[0] }}" href="https://{{ lang[0] }}.hackndo.com{{ post.url }}"/>
        {% endif %}
      {% endfor %}
    {% endif %}

    {% if post.last_modified_at %}
      <lastmod>{{ post.last_modified_at | date_to_xmlschema }}</lastmod>
    {% elsif post.sitemap.lastmod %}
      <lastmod>{{ post.sitemap.lastmod | date: "%Y-%m-%d" }}</lastmod>
    {% elsif post.date %}
      <lastmod>{{ post.date | date_to_xmlschema }}</lastmod>
    {% else %}
      <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    {% endif %}
  </url>
  {% endfor %}
  <url>
    <loc>{{ site.url }}/</loc>
    <xhtml:link rel="alternate" hreflang="fr" href="https://beta.hackndo.com/"/>
    <xhtml:link rel="alternate" hreflang="en" href="https://en.hackndo.com/"/>
    <xhtml:link rel="alternate" hreflang="x-default" href="https://beta.hackndo.com/"/>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
  </url>

  {% for page in site.pages %}
    {% unless page.sitemap.exclude == 'yes' or page.url contains '.html' or page.url contains '.xml' or page.url contains '.txt' %}
      {% if page.layout %}
  <url>
    <loc>{{ site.url }}{{ page.url }}</loc>
    <lastmod>{% if page.last_modified_at %}{{ page.last_modified_at | date_to_xmlschema }}{% else %}{{ site.time | date_to_xmlschema }}{% endif %}</lastmod>
  </url>
      {% endif %}
    {% endunless %}
  {% endfor %}
</urlset>